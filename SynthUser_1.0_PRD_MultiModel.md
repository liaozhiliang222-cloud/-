# SynthUser 1.0 产品需求文档（PRD）

## 版本：v1.0 — 免费版 PWA 需求验证

> 定位：AI 合成用户研究一键生成器  
> 形态：纯前端 PWA（零后端、零服务器、用户自带 API Key）  
> 核心：输入研究主题，3 分钟获得虚拟用户访谈笔录或问卷统计数据  
> 目标：验证需求，收集反馈，为 Pro 版定价提供数据支撑

---

## 1. 产品概述

### 1.1 核心概念

SynthUser 是一款面向市场研究员、产品经理、创业者的**纯前端 AI 工具**。用户输入研究主题和问题，通过调用大语言模型（Kimi API）生成两类虚拟研究数据：

- **定性模式**：6 位虚拟受访者的深度访谈笔录
- **定量模式**：100 位虚拟受访者的问卷回答及统计结果

所有操作在浏览器内完成，数据不过服务器，结果可复制带走。

### 1.2 免费版约束（明确边界）

| 约束项 | 免费版策略 | 原因 |
|--------|-----------|------|
| **API Key** | 用户自带 Kimi API Key，存于本地 localStorage | 零成本启动，零后端维护 |
| **历史记录** | 不保存，刷新页面即清空 | 零数据库，降低复杂度 |
| **生成次数** | 不限次数（受用户自身 API 额度限制） | 让用户充分试用 |
| **导出格式** | 纯文本复制（Markdown / CSV） | 够用即可，不做复杂导出 |
| **用户数量** | 定性 6 人 / 定量 100 人 | 固定配额，简化逻辑 |
| **题目数量** | 定性 3 题 / 定量 3-5 题 | 限制范围，保证输出质量 |

### 1.3 核心原则

1. **极简闭环**：从输入到结果，不超过 3 次点击
2. **零留存**：不保存用户数据，结果复制带走，保护隐私
3. **透明可控**：展示完整 Prompt，用户可修改，知道 AI 在做什么
4. **方法论驱动**：每个输出格式都符合市场研究行业规范

---

## 2. 目标用户与使用场景

### 2.1 目标用户

| 用户类型 | 典型场景 | 痛点 |
|---------|---------|------|
| **市场研究员（乙方）** | 提案阶段快速验证方向，给客户看初步假设 | 座谈会招募慢，前期缺乏方向感 |
| **用户研究员（甲方）** | 产品上线前快速验证概念，排优先级 | 真实用户招募周期长，错过窗口 |
| **产品经理** | 新品概念测试，功能优先级排序 | 没有早期用户反馈，依赖主观判断 |
| **创业者/独立顾问** | 低成本验证商业假设，写商业计划书 | 没有预算做真实调研 |
| **高校师生** | 课程作业、毕业论文预研究 | 样本获取难，预算有限 |

### 2.2 使用场景

#### 场景 A：概念测试（定性）

> 产品经理小李要做一款"0 糖气泡水"，想快速了解目标用户的接受度。打开 SynthUser，输入主题和 3 个问题，3 分钟后获得 6 位虚拟用户的访谈笔录，复制到飞书文档分享给团队讨论。

#### 场景 B：假设验证（定量）

> 研究员小王需要验证"价格敏感度是否因城市层级不同而存在差异"。在 SynthUser 设定一个 5 题问卷（含单选和量表），生成 100 份虚拟回答，看交叉趋势是否符合预期，再决定是否投入真实问卷投放。

#### 场景 C：提案预研（定性+定量）

> 乙方顾问小张要给客户提案，但没有预算做预调研。用 SynthUser 快速生成虚拟座谈会笔录，作为提案中的"初步发现"支撑，同时用问卷模拟器验证假设方向。

---

## 3. 功能需求

### 3.1 信息架构（仅 3 个页面）

```
┌─────────────────┐
│   Page 1: 探索   │  ← 6 个示例卡片 + 创建入口
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Page 2: 创建   │  ← Tab 切换（定性/定量）+ 表单
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Page 3: 结果   │  ← 生成结果 + 复制操作
└─────────────────┘
```

---

### 3.2 Page 1: 探索页

**目标**：降低首次使用门槛，让用户"先体验，再创建"。

#### 3.2.1 页面布局

| 区域 | 内容 | 说明 |
|------|------|------|
| **顶部** | Logo + Slogan | "SynthUser — 3 分钟获得虚拟用户洞察" |
| **中部** | 6 个示例卡片 | 横向滑动或 2×3 网格布局 |
| **底部** | 主按钮 | "创建我的研究"（大号绿色按钮） |
| **底部次级** | 使用说明链接 | "如何使用？"（展开说明） |

#### 3.2.2 示例卡片（6 个）

每个卡片包含：
- 标题（研究主题）
- 副标题（目标人群）
- 3 个关键词标签
- 点击后直接进入 **Page 3 结果页**，使用预置 Prompt 生成

**示例内容**：

| # | 标题 | 人群 | 标签 |
|---|------|------|------|
| 1 | 海外用户对短视频电商的接受度 | 北美/欧洲/日韩用户 | 跨境电商、短视频、消费习惯 |
| 2 | 抖音主播为什么做直播 | 抖音全职/兼职主播 | 直播动机、收入、粉丝运营 |
| 3 | 小红书用户有哪些社交需求 | 小红书活跃用户 | 内容偏好、互动、社区氛围 |
| 4 | 新能源汽车购买决策因素 | 一线城市潜在购车者 | 续航、价格、品牌、充电 |
| 5 | 0 糖气泡水概念测试 | 年轻白领/健康人群 | 口味、价格、健康、场景 |
| 6 | 母婴用品选购痛点 | 0-3 岁宝宝妈妈 | 安全、价格、品牌、渠道 |

#### 3.2.3 交互逻辑

| 动作 | 结果 |
|------|------|
| 点击示例卡片 | 跳转 Page 3，自动加载预置 Prompt，开始生成（需用户已填 Key，否则先弹窗提示） |
| 点击"创建我的研究" | 跳转 Page 2 |
| 首次访问 | 自动展开"使用说明"，引导用户输入 API Key |

---

### 3.3 Page 2: 创建页

**目标**：让用户快速配置研究任务，两种模式共用同一套 UI 框架。

#### 3.3.1 页面布局

| 区域 | 内容 |
|------|------|
| **顶部** | 返回按钮 + 页面标题"创建研究" |
| **Tab 栏** | 【虚拟座谈会】|【问卷模拟器】|
| **表单区** | 根据 Tab 切换显示不同表单 |
| **底部** | API Key 输入区 + 生成按钮 |

#### 3.3.2 模式 A：虚拟座谈会（定性）

| 字段 | 类型 | 必填 | 默认值/说明 |
|------|------|------|------------|
| 研究主题 | 单行文本输入 | 是 | placeholder: "例如：测试一款 0 糖气泡水的概念" |
| 问题 1 | 多行文本输入 | 是 | 默认："你对这个产品的第一印象是什么？" |
| 问题 2 | 多行文本输入 | 是 | 默认："什么情况下你会考虑购买或使用？" |
| 问题 3 | 多行文本输入 | 是 | 默认："最大的顾虑或阻碍是什么？" |
| 用户群体 | 单选按钮组 | 是 | 选项：普通消费者 / 年轻白领 / 精致妈妈 / 专业人士 |
| 生成人数 | 固定展示 | — | 显示"将生成 6 位虚拟用户"（不可改） |

#### 3.3.3 模式 B：问卷模拟器（定量）

| 字段 | 类型 | 必填 | 默认值/说明 |
|------|------|------|------------|
| 研究主题 | 单行文本输入 | 是 | placeholder: "例如：0 糖气泡水购买意愿调查" |
| 题目列表 | 动态表单 | 是 | 默认 3 题，可增删（最多 5 题） |
| 题目类型 | 每题内单选 | 是 | ① 单选题 / ② 量表题 |
| 单选选项 | 文本输入 | 条件 | 选"单选题"时显示，输入 A/B/C/D 选项文本 |
| 量表范围 | 单选 | 条件 | 选"量表题"时显示：1-5 分 / 1-7 分 |
| 用户群体 | 单选按钮组 | 是 | 同定性模式 |
| 样本量 | 固定展示 | — | 显示"将模拟 100 位受访者"（不可改） |

#### 3.3.4 API Key 与模型选择区（页面底部固定）

| 元素 | 说明 |
|------|------|
| **模型选择** | 下拉菜单：Kimi（默认）/ DeepSeek / 智谱 GLM / 自定义 |
| **API Key 输入** | 密码类型输入（可切换显示/隐藏），placeholder 根据所选模型动态变化 |
| **Base URL（自定义时显示）** | 输入框，供自托管或代理用户使用 |
| **保存按钮** | 存入 localStorage，标记为"已保存" |
| **帮助链接** | "如何获取 API Key？"（跳转对应平台文档） |
| **隐私提示** | 小字："Key 仅保存在你的浏览器本地，不会上传任何服务器" |

**已保存状态**：如果 localStorage 已有配置，自动填充并显示"已保存"标签。

**模型切换逻辑**：切换模型时，清空当前 Key 输入框（避免混用），保留其他表单内容。

#### 3.3.5 生成按钮

| 状态 | 表现 |
|------|------|
| 未填必填项 | 按钮置灰，不可点击 |
| 已填但未保存 Key | 按钮显示"请保存 API Key" |
| 就绪 | 绿色按钮："开始生成 — 预计 1-2 分钟" |
| 生成中 | 按钮变加载态，显示进度："正在访谈用户 3/6..." |

#### 3.3.6 交互逻辑

| 动作 | 结果 |
|------|------|
| 切换 Tab | 保留已填内容（主题、群体、Key），清空题目相关字段 |
| 点击生成 | 校验必填项 → 校验 Key → 跳转 Page 3 → 开始 API 调用 |
| 生成失败 | Page 3 顶部显示错误信息（API 错误/额度不足/网络问题），提供"重试"和"返回修改" |

---

### 3.4 Page 3: 结果页

**目标**：清晰展示生成结果，支持一键复制带走。

#### 3.4.1 页面布局（通用顶部）

| 区域 | 内容 |
|------|------|
| **顶部导航** | 返回按钮（回 Page 2）+ 研究主题标题 |
| **元信息栏** | 模式标签（座谈会/问卷）+ 用户群体 + 生成时间 |
| **操作栏** | 【复制全部】+【重新生成】+【修改问题】 |
| **主体区** | 根据模式显示不同内容 |

#### 3.4.2 模式 A 结果：虚拟座谈会

**展示形式**：纵向排列的 6 个用户卡片。

**单个用户卡片结构**：

```
┌─────────────────────────────────────┐
│ [头像 emoji] 姓名 · 年龄 · 职业标签   │
├─────────────────────────────────────┤
│ Q1: [问题摘要]                       │
│ A1: [回答内容，2-4 句话]             │
│                                      │
│ Q2: [问题摘要]                       │
│ A2: [回答内容]                       │
│                                      │
│ Q3: [问题摘要]                       │
│ A3: [回答内容]                       │
└─────────────────────────────────────┘
```

**卡片设计规范**：
- 头像：固定 emoji（👩 👨 👩‍💼 👨‍💼 👩‍🔬 👨‍🔬），按顺序分配
- 姓名：AI 生成中文名（如"林晓婷""王建国"）
- 年龄：AI 根据群体生成合理年龄
- 职业标签：一句话描述（如"一线城市精致妈妈，注重成分"）
- 回答：口语化、有细节、有情绪，避免模板感

**操作栏**：

| 按钮 | 功能 |
|------|------|
| **复制全部** | 复制为 Markdown 格式（含标题、用户画像、问答对） |
| **重新生成** | 使用相同参数重新调用 API，换一批用户 |
| **修改问题** | 返回 Page 2，保留已填内容，可修改 |

**Markdown 复制格式示例**：

```markdown
# 0 糖气泡水概念测试 — 虚拟座谈会笔录

## 用户画像：精致妈妈群体

### 用户 1：林晓婷（32 岁，上海）
**标签**：注重成分、价格敏感

**Q1：你对这个产品的第一印象是什么？**
A1：...

**Q2：什么情况下你会考虑购买？**
A2：...

**Q3：最大的顾虑是什么？**
A3：...

---

### 用户 2：...
```

#### 3.4.3 模式 B 结果：问卷模拟器

**展示形式**：每个题目一个统计卡片，纵向排列。

**单选题卡片结构**：

```
┌─────────────────────────────────────┐
│ 题目 1：你会购买这款 0 糖气泡水吗？   │
├─────────────────────────────────────┤
│ ▓▓▓▓▓▓▓▓░░  一定会   45%          │
│ ▓▓▓▓▓▓░░░░░  可能会   30%          │
│ ▓▓▓░░░░░░░░  不确定   15%          │
│ ▓▓░░░░░░░░░  不会     10%          │
├─────────────────────────────────────┤
│ 样本量：100  |  缺失：0              │
└─────────────────────────────────────┘
```

**量表题卡片结构**：

```
┌─────────────────────────────────────┐
│ 题目 2：你对健康饮品的重视程度（1-5） │
├─────────────────────────────────────┤
│ 均值：3.8  |  标准差：0.9            │
│                                      │
│ 1 分：▓▓  8%                         │
│ 2 分：▓▓▓▓  15%                      │
│ 3 分：▓▓▓▓▓▓  25%                    │
│ 4 分：▓▓▓▓▓▓▓▓▓▓  35%               │
│ 5 分：▓▓▓▓▓▓▓  17%                  │
└─────────────────────────────────────┘
```

**图表实现**：纯 CSS 条形图（`div` + `width` 百分比），不引入图表库，保证轻量。

**操作栏**：

| 按钮 | 功能 |
|------|------|
| **复制全部** | 复制为 CSV 格式（含题目、选项、频数、百分比） |
| **重新生成** | 重新调用 API，换一批虚拟样本 |
| **修改问题** | 返回 Page 2 |

**CSV 复制格式示例**：

```csv
题目,选项,频数,百分比
"你会购买吗？","一定会",45,45%
"你会购买吗？","可能会",30,30%
"你会购买吗？","不确定",15,15%
"你会购买吗？","不会",10,10%
"重视程度","1分",8,8%
...
```

---

### 3.5 全局功能

#### 3.5.1 API Key 管理

| 功能 | 说明 |
|------|------|
| 存储位置 | localStorage（`synthuser_api_key`） |
| 显示策略 | 默认隐藏（******），点击眼睛图标可查看 |
| 修改 | 用户可随时在 Page 2 底部修改，覆盖旧值 |
| 清除 | 提供"清除 Key"按钮，删除 localStorage 项 |

#### 3.5.2 错误处理

| 错误类型 | 表现 | 用户操作 |
|---------|------|---------|
| API Key 无效 | Page 3 顶部红色提示："API Key 无效，请检查" | 返回 Page 2 修改 Key |
| API 额度不足 | 提示："Kimi 账户额度不足，请充值" | 跳转 Kimi 充值页面 |
| 网络超时 | 提示："请求超时，请检查网络后重试" | 点击重试 |
| 生成内容异常 | 提示："AI 返回格式异常，请重试" | 点击重新生成 |
| 内容安全拦截 | 提示："研究主题涉及敏感内容，请修改后重试" | 返回修改主题 |

#### 3.5.3 加载与等待

| 状态 | 设计 |
|------|------|
| 生成中 | 页面中央显示动画（3 个点脉冲）+ 文字："正在生成虚拟用户...（3/6）" |
| 预计时间 | 定性约 30-60 秒，定量约 60-90 秒（根据模型响应速度） |
| 取消 | 提供"取消生成"按钮，中断请求，返回 Page 2 |

---

## 4. 技术架构

### 4.1 技术栈（纯前端）

| 层级 | 选型 | 说明 |
|------|------|------|
| **框架** | React 18 + Vite | 快速开发，热更新 |
| **路由** | react-router-dom | 3 个页面路由 |
| **状态管理** | React Context / useState | 无复杂全局状态，不用 Redux |
| **样式** | Tailwind CSS | 原子化 CSS，快速出 UI |
| **图表** | 纯 CSS 条形图 | 定量结果用 div 宽度模拟，零图表库 |
| **HTTP 请求** | 原生 fetch | 直接调用 Kimi API |
| **数据解析** | 正则 + JSON.parse | 解析 AI 返回的 Markdown/JSON |
| **构建** | Vite | 打包 PWA |
| **部署** | Vercel / GitHub Pages | 免费静态托管 |

### 4.2 PWA 配置

```json
// manifest.json
{
  "name": "SynthUser — AI 合成用户研究",
  "short_name": "SynthUser",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#0A192F",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192" },
    { "src": "/icon-512.png", "sizes": "512x512" }
  ]
}
```

**Service Worker**：使用 Vite PWA 插件自动生成，缓存静态资源，支持离线访问（但生成需联网）。

### 4.3 API 调用方案（多模型支持）

**模型配置表**：

| 模型 | Base URL | 默认模型名 | 认证方式 | 特点 |
|------|----------|-----------|---------|------|
| **Kimi** | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` | Bearer Token | 长文本支持好，中文语境强 |
| **DeepSeek** | `https://api.deepseek.com/v1` | `deepseek-chat` | Bearer Token | 推理能力强，性价比高 |
| **智谱 GLM** | `https://open.bigmodel.cn/api/paas/v4` | `glm-4` | Bearer Token | 中文理解细腻，合规稳定 |
| **自定义** | 用户输入 | 用户输入 | Bearer Token | 兼容 OpenAI 格式的任意模型 |

**前端调用逻辑**：

```javascript
// 模型配置中心
const MODEL_CONFIG = {
  kimi: {
    baseURL: 'https://api.moonshot.cn/v1',
    defaultModel: 'moonshot-v1-8k',
    docURL: 'https://platform.moonshot.cn/docs'
  },
  deepseek: {
    baseURL: 'https://api.deepseek.com/v1',
    defaultModel: 'deepseek-chat',
    docURL: 'https://platform.deepseek.com/docs'
  },
  zhipu: {
    baseURL: 'https://open.bigmodel.cn/api/paas/v4',
    defaultModel: 'glm-4',
    docURL: 'https://open.bigmodel.cn/dev/api'
  },
  custom: {
    baseURL: '', // 用户输入
    defaultModel: '', // 用户输入
    docURL: ''
  }
};

// 统一调用函数
async function callLLM(provider, apiKey, model, messages) {
  const config = MODEL_CONFIG[provider];
  const baseURL = provider === 'custom' ? localStorage.getItem('custom_base_url') : config.baseURL;
  const modelName = provider === 'custom' ? localStorage.getItem('custom_model') : (model || config.defaultModel);

  const response = await fetch(`${baseURL}/chat/completions`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${apiKey}`
    },
    body: JSON.stringify({
      model: modelName,
      messages: messages,
      temperature: 0.7,
      stream: false // 1.0 版先不支持流式，简化处理
    })
  });

  return response.json();
}
```

**注意**：由于浏览器 CORS 限制，部分模型 API 可能不支持前端直调。若遇到跨域问题，提供两种降级方案：
1. 用户安装浏览器 CORS 插件（开发调试期）
2. 后续补充 Cloudflare Workers 免费中转（生产环境）

### 4.4 Prompt 工程（核心资产）

**模型适配原则**：所有 Prompt 采用 OpenAI 兼容格式（`system` + `user` 角色），国产模型（Kimi/DeepSeek/智谱）均原生支持。若使用自定义模型，需确保其支持 Chat Completions 格式。

**各模型特性差异**：

| 模型 | 定性生成特点 | 定量生成特点 | 建议 |
|------|------------|------------|------|
| **Kimi** | 中文口语化自然，用户画像细腻 | 数据分布合理，格式遵循度高 | 默认首选，综合表现稳定 |
| **DeepSeek** | 推理深入，观点有层次感 | 逻辑严谨，量表分布符合统计直觉 | 适合需要深度洞察的定性研究 |
| **智谱 GLM** | 中文表达地道，场景感强 | 格式规范，适合结构化输出 | 适合概念测试等创意类研究 |

#### 4.4.1 定性模式 Prompt

```
【系统指令】
你是一位资深市场研究主持人，正在主持一场关于"{研究主题}"的焦点小组座谈会。
请扮演 6 位不同的{用户群体}，分别回答以下 3 个问题。

要求：
1. 每位用户必须有：姓名、年龄、城市、一句话标签
2. 回答必须口语化、有具体场景、有情绪，避免模板感
3. 6 位用户之间要有明显差异（乐观/谨慎/价格敏感/品质导向等）
4. 每位用户对 3 个问题的回答总字数控制在 200-300 字

【问题】
Q1: {问题1}
Q2: {问题2}
Q3: {问题3}

【输出格式】
请用 Markdown 格式输出，每位用户一个二级标题：

## 用户 1：姓名（年龄，城市）
**标签**：一句话描述

**Q1: {问题1}**
A1: [回答]

**Q2: {问题2}**
A2: [回答]

**Q3: {问题3}**
A3: [回答]

---

（以此类推，共 6 位用户）
```

#### 4.4.2 定量模式 Prompt

```
【系统指令】
你是一位数据模拟专家。请模拟 100 位{用户群体}对以下问卷的回答。
要求：
1. 回答分布必须符合真实消费行为的统计规律（如极端选项占比通常较低）
2. 不同题目之间要有合理的相关性（如"重视健康"与"愿意溢价购买"应正相关）
3. 输出 100 行原始数据 + 每题的统计汇总

【问卷题目】
{题目列表，含题型和选项}

【输出格式】
第一部分：原始数据（CSV 格式，100 行）
UserID,题目1,题目2,题目3,...
1,A,5,B,...
2,B,3,C,...
...

第二部分：统计汇总（每题一个 JSON 块）
```json
{
  "题目1": {
    "类型": "单选",
    "选项": {"A": 45, "B": 30, "C": 15, "D": 10},
    "总计": 100
  },
  "题目2": {
    "类型": "量表",
    "均值": 3.8,
    "标准差": 0.9,
    "分布": {"1": 8, "2": 15, "3": 25, "4": 35, "5": 17}
  }
}
```
```

---

## 5. 数据流与隐私

### 5.1 数据生命周期

```
用户输入（主题/问题/Key）
    ↓
浏览器 localStorage（仅 Key）
    ↓
前端组装 Prompt
    ↓
fetch 请求 → Kimi API（用户浏览器直发）
    ↓
AI 响应
    ↓
前端解析 → 渲染到 Page 3
    ↓
用户复制/关闭页面
    ↓
数据销毁（不保存，刷新即无）
```

### 5.2 隐私声明（页面底部常驻）

> 🔒 **隐私说明**：你的 API Key 仅保存在本地浏览器中，研究主题和结果不会上传任何服务器。刷新页面后结果将清空，请记得及时复制保存。

---

## 6. UI/UX 设计规范

### 6.1 视觉风格

| 要素 | 规范 |
|------|------|
| **主色调** | 深空蓝 `#0A192F`（专业、沉稳）+ 研究绿 `#2EB75B`（行动按钮） |
| **背景色** | 白色 `#FFFFFF` + 浅灰 `#F8F9FA`（卡片背景） |
| **字体** | 系统默认无衬线字体（PingFang SC / Microsoft YaHei） |
| **字号** | 标题 20px / 正文 16px / 辅助 14px / 标签 12px |
| **圆角** | 卡片 12px / 按钮 8px / 输入框 6px |
| **阴影** | 卡片 `0 2px 8px rgba(0,0,0,0.08)` |

### 6.2 响应式适配

| 设备 | 布局 |
|------|------|
| **桌面端** | 居中容器，最大宽度 720px，两侧留白 |
| **平板** | 同桌面端，容器宽度 90% |
| **手机** | 单列全宽，卡片纵向排列，字号略大（18px 正文） |

---

## 7. 部署方案

### 7.1 免费部署（Vercel）

| 步骤 | 操作 |
|------|------|
| 1. 代码推送到 GitHub | 创建公开/私有仓库 |
| 2. 登录 Vercel | 用 GitHub 账号登录 vercel.com |
| 3. 导入项目 | 选择仓库，框架预设 React + Vite |
| 4. 构建配置 | Build Command: `npm run build` / Output Directory: `dist` |
| 5. 部署 | 点击 Deploy，30 秒后获得 `https://synthuser.vercel.app` |
| 6. 自定义域名（可选） | 绑定自己的域名（如 `synthuser.app`），需 DNS 解析 |

### 7.2 备选部署（GitHub Pages）

| 步骤 | 操作 |
|------|------|
| 1. 构建 | `npm run build`，确保输出到 `dist` |
| 2. 配置 | `vite.config.ts` 设置 `base: '/仓库名/'` |
| 3. 部署 | 使用 `gh-pages` 分支或 GitHub Actions 自动部署 |
| 4. 访问 | `https://你的用户名.github.io/仓库名` |

### 7.3 国内访问优化

| 问题 | 方案 |
|------|------|
| Vercel 域名被墙 | 绑定自定义域名（海外域名 + Cloudflare DNS） |
| GitHub Pages 慢 | 使用 jsDelivr 加速或国内 CDN |
| Kimi API 访问 | 通常国内直连正常，无需代理 |

---

## 8. 里程碑（4 周上线）

| 周次 | 目标 | 产出 |
|------|------|------|
| **W1** | 环境搭建 + Page 1（探索页） | Vite+React 项目跑通，6 个示例卡片 UI 完成，点击跳转逻辑 |
| **W2** | Page 2（创建页）+ API 接入 | 双模式表单完成，Kimi API 调通，能返回 Markdown/JSON |
| **W3** | Page 3（结果页）+ 复制功能 | 定性卡片渲染、定量 CSS 图表、复制 Markdown/CSV 功能 |
| **W4** | PWA 配置 + 优化 + 部署 | manifest + Service Worker、错误处理、加载态、Vercel 部署、自测 6 个示例 |

---

## 9. 验证指标（上线后追踪）

| 指标 | 目标值 | 追踪方式 |
|------|--------|---------|
| **页面访问量（PV）** | 首周 >500 | Vercel Analytics / Google Analytics |
| **示例点击率** | >30% | 前端埋点（简单的 console.log 或 localStorage 计数） |
| **创建转化率** | >15% | 从 Page 1 到 Page 2 的点击比例 |
| **生成完成率** | >60% | 从点击生成到成功展示结果的比例 |
| **复制使用率** | >40% | 点击"复制全部"的用户占比 |
| **定性/定量使用比例** | — | 观察哪种模式更受欢迎 |
| **用户反馈收集** | >20 条 | 页面底部固定反馈入口（腾讯问卷/金数据链接） |

---

## 10. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| **模型 API 不支持前端 CORS** | 高 | 先用浏览器 CORS 插件方案验证；若不可行，W2 切换为 Cloudflare Workers 免费中转。优先测试 Kimi/DeepSeek/智谱的 CORS 支持情况 |
| **AI 生成结果质量不稳定** | 高 | 持续优化 Prompt；提供"重新生成"按钮；W4 增加"结果评分"收集反馈；根据模型特性调整 Prompt（如 DeepSeek 可适当减少约束，发挥其推理优势） |
| **用户没有 API Key** | 中 | 页面显著位置放各平台"如何获取 Key"教程（Kimi/DeepSeek/智谱分开展示）；考虑后续提供"代调用"的 Pro 版 |
| **模型切换导致结果不一致** | 低 | 页面提示"不同模型生成风格可能不同，建议同一项目使用同一模型" |
| **生成时间过长导致用户流失** | 中 | 增加有趣的等待文案（如"正在邀请用户入场..."）；提供取消按钮 |
| **结果格式解析失败** | 中 | 增加健壮的正则解析；解析失败时展示原始文本，不白屏 |
| **被恶意刷 API** | 低 | 免费版无成本，刷的是用户自己的 Key；Pro 版再考虑限流 |

---

*文档版本：v1.0*  
*日期：2026-06-29*  
*状态：待开发*
