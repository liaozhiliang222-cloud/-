{
  "name": "synthuser-pwa-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "deploy:cf": "wrangler pages deploy . --project-name=sym --branch=main",
    "deploy:cf:preview": "wrangler pages deploy . --project-name=sym --branch=preview"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
